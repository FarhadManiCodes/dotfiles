[Unit]
Description=Daily sync of study library

[Timer]
# Run daily at 6 AM
OnCalendar=*-*-* 06:00:00
# Also run 2 minutes after login
OnStartupSec=2min
# Catch up if missed (laptop was off)
Persistent=true

[Install]
WantedBy=timers.target
