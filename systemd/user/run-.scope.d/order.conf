# Keep podman's DNS helper (aardvark-dns, via systemd-run) until pg.service has stopped: its cleanup needs it,
# and no new scope can start during shutdown.
# This prefix matches any systemd-run --scope: such a scope stops after pg, and one started while pg
# is stopping waits for that stop (see revisit.md).
[Unit]
Before=pg.service
