# --- OS & Environment ---
.DS_Store
.env
.env.*
!.env.example
*.log

# --- Python / Data Science ---
__pycache__/
*.py[cod]
*$py.class
.ipynb_checkpoints/
.venv/
venv/
ENV/

# --- IDEs & Editors ---
.vscode/
.idea/
*.swp
*.swo
*~

# --- Data Engineering (Large Files & Formats) ---
# Prevent accidental commit of big data
*.parquet
*.avro
*.h5
*.pkl
*.fth
*.orc

# --- dbt (Data Build Tool) ---
/target/
/dbt_packages/
/logs/

# --- C / C++ ---
# Object files
*.o
*.ko
*.obj
*.elf

# Libraries
*.a
*.lib
*.so
*.so.*
*.dylib
*.dll

# Executables
*.exe
*.out
*.app
a.out

# --- CMake ---
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CMakeScripts/
# NB: 'Makefile' is intentionally NOT ignored globally — a hand-written
# top-level Makefile must stay trackable. CMake/Ninja generated build files
# live under the ignored build dirs below.
# Common build directory names
build/
_build/
build_*/
bin/

# --- Local / private git config overrides (defensive) ---
# Real identity lives in ~/.config/git/config.local (untracked, outside any
# worktree). Belt-and-suspenders guard against a stray local git config
# override getting committed inside a repo.
.gitconfig.local

**/.claude/settings.local.json
