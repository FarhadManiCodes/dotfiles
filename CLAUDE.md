# CLAUDE.md

Guidance for Claude Code in this dotfiles repository. The guardrails are shared with Codex
and live in one file, imported here so there is only ever one copy to keep correct:

@AGENTS.md

Detailed reference, read on demand rather than loaded into every session:
`docs/architecture.md` (per-application rationale and operational constraints) and
`docs/system-notes.md` (installation, verifying claims, environment, package decisions).
